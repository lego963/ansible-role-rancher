---
dependency:
    name: galaxy
driver:
    name: docker
lint: |
    yamllint .
    ansible-lint
    flake8
platforms:
    - name: instance
      image: "python:3.7"
      pre_build_image: true
      privileged: true
provisioner:
    name: ansible
    playbooks:
        converge: converge.yml
    log: true
